# Copy this file to config_local.py and set your MySQL password.
# Workbench may connect without asking, but Python needs the password.

DB_OVERRIDES = {
    "password": "PUT_YOUR_MYSQL_PASSWORD_HERE",
}
